{
  "scripts": {
    "start": "npm run --prefix backend start",
    "dev": "npx http-server frontend -a 127.0.0.1 -p 8080 -c-1",
    "backend": "npm run --prefix backend dev"
  },
  "dependencies": {
    "i": "^0.3.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
